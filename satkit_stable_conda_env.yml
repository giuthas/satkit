name: satkit-stable
channels:
  - conda-forge 
  - defaults
dependencies:
  - python >= 3.10
  - numpy >= 1.22
  - scipy >= 1.7
  - dipy >= 1.4
  - opencv >= 4.5
  - matplotlib >= 3.5
  - pylint >= 2.9
  - pip 
  - pip:
    - praat-textgrids >= 1.3

