name: satkit-devel
channels:
  - conda-forge 
  - defaults
dependencies:
  - python >= 3.10
  - numpy >= 1.22
  - scipy >= 1.7
  - dipy >= 1.4
  - opencv >= 4.5
  - sk-video >= 1.1
  - matplotlib >= 3.5
  - pylint >= 2.9
  - setuptools >= 58
  - build 
  - virtualenv
  - pip >= 22
  - pip:
    - praat-textgrids >= 1.3
